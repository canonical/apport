#!/bin/sh -ex
CRASHDUMP=/proc/sys/kernel/crashdump-helper
[ -e $CRASHDUMP ] || CRASHDUMP=/proc/sys/kernel/crashdump
python problem_report.py -v
python apport_utils.py -v
if [ -e $CRASHDUMP ] && [ -n "`cat $CRASHDUMP`" ]; then
    ./test-apport kernel
else
    ./test-apport lib
fi
