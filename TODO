apport:

problem_report.py:

apport-gtk:
 - drag&drop URL
 - add gksudo wrapper for disabling apport, and checkbox to crash
   dialog
 - check delay of progress dialog when filing a bug
 - fetch cloakroom ticket number from HTTP headers

kernel hook:

general:
 - apport-retrace: fetch comment and attachments from bug URL
   (bughelper already has code for that)
